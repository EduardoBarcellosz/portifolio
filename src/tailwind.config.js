/** @type {import('tailwindcss').Config} */
export const content = [
  "./src/**/*.{html,js,ts,jsx,tsx}", // Inclua todos os arquivos HTML, JS, TS, JSX, e TSX dentro da pasta src
];
export const theme = {
  extend: {},
};
export const plugins = [];

